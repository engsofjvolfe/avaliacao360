# Planilhas reais/preenchidas — nunca versionadas (dado real ou nomes fictícios de
# desenvolvimento). Só o modelo vazio (planilhas/Avaliacao360-Modelo.xlsx) é versionado.
planilhas/Avaliacao360_Ligamento.xlsx
planilhas/Avaliacao360_Ligamento.limpa.xlsx
planilhas/Avaliacao360_Ligamento.backup-antes-fix.xlsx
planilhas/*.backup*.xlsx

# Documentação interna de manutenção — não faz parte do repositório público
# (continua existindo localmente, só não é versionada).
docs/NOTAS-TECNICAS.md
docs/ARQUIVOS-ALTERADOS.md
docs/POSSIBILIDADES-PAINEL-WEB.md

# Dependências instaladas por `npm install` — reproduzíveis a partir do
# package-lock.json, que fica versionado.
node_modules/

# Artefatos gerados pelos testes (HTML processado) — reproduzíveis rodando
# `npm test`, não precisam ficar versionados.
tests/fixtures/PainelDialog.rendered.html

# Sistema operacional / editor
.DS_Store
Thumbs.db
*.swp
